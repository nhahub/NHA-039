# مشروع Shoppia - دليل شامل

## نظرة عامة على المشروع

Shoppia هو تطبيق ويب لإدارة المتاجر الإلكترونية مبني باستخدام ASP.NET Core MVC مع Entity Framework Core وSQL Server.

---

## 🏗️ بنية المشروع (Project Structure)

### 1. **المجلدات الرئيسية**

```
Shoppia/
├── Controllers/      # التحكم في طلبات المستخدم والاستجابة
├── Models/          # نماذج قاعدة البيانات
├── Views/           # واجهات المستخدم (HTML)
├── ViewModels/      # نماذج خاصة بالعرض
├── wwwroot/         # الملفات الثابتة (CSS, JS, صور)
└── Program.cs       # نقطة البداية للتطبيق
```

---

## 📊 قاعدة البيانات (Database Models)

### الجداول الموجودة:

#### 1. **Category (الفئات)**

- تخزين تصنيفات المنتجات (مثل: إلكترونيات، ملابس، إلخ)
- الحقول: `CategoryId`, `Name`

#### 2. **Product (المنتجات)**

- تخزين معلومات المنتجات المتاحة للبيع
- الحقول: `ProductId`, `Name`, `Price`, `Photo`, `StockQuantity`, `CategoryId`
- مرتبط بـ: `Category` (فئة واحدة لكل منتج)

#### 3. **Client (العملاء)**

- معلومات العملاء الذين يشترون من المتجر
- الحقول: `ClientId`, `Name`, `Mobile`
- مرتبط بـ: `Orders` (العميل يمكنه إنشاء عدة طلبات)

#### 4. **ClientAddress (عناوين العملاء)**

- عناوين التوصيل للعملاء
- الحقول: `ClientAddressId`, `ClientId`, `Address`
- مرتبط بـ: `Client`

#### 5. **Order (الطلبات)**

- طلبات الشراء من العملاء
- الحقول: `OrderId`, `OrderDate`, `ClientId`, `TotalAmount`, `OrderStatusId`, `ClientAddressesId`
- مرتبط بـ: `Client`, `OrderStatus`, `OrderItems`

#### 6. **OrderItem (عناصر الطلب)**

- المنتجات داخل كل طلب
- الحقول: `OrderItemId`, `OrderId`, `ProductId`, `Quantity`, `Price`
- مرتبط بـ: `Order`, `Product`

#### 7. **OrderStatus (حالة الطلب)**

- حالات الطلبات (مثل: قيد المعالجة، تم الشحن، تم التسليم)
- الحقول: `OrderStatusId`, `Name`

#### 8. **Vendor (الموردون)**

- معلومات الموردين الذين يزودون المتجر بالمنتجات
- الحقول: `VendorId`, `Name`, `Email`, `Mobile`, `Address`, `IsActive`, `IsBlacklist`

#### 9. **SupplyOrder (أوامر التوريد)**

- طلبات الشراء من الموردين
- الحقول: `SupplyOrderId`, `Total`, `SupplyDate`, `VendorId`
- مرتبط بـ: `Vendor`, `SupplyOrderDetails`

#### 10. **SupplyOrderDetail (تفاصيل أوامر التوريد)**

- المنتجات داخل كل أمر توريد
- الحقول: `SupplyOrderDetailId`, `SupplyOrderId`, `ProductId`, `Quantity`, `Price`

#### 11. **ApplicationUser (المستخدمين)**

- حسابات المستخدمين في النظام
- يستخدم ASP.NET Core Identity للمصادقة
- الحقول: `Id`, `UserName`, `Email`, `PasswordHash`, وحقول Identity الأخرى

---

## 🎮 المتحكمات (Controllers)

### كيف تعمل المتحكمات؟

المتحكم يستقبل طلب من المستخدم، يتعامل مع البيانات، ثم يرسل النتيجة للعرض (View).

### المتحكمات الموجودة:

#### 1. **HomeController**

- الصفحة الرئيسية وصفحة الخصوصية
- متاح للجميع (لا يحتاج تسجيل دخول)

#### 2. **AccountController**

- **Register**: إنشاء حساب جديد
- **Login**: تسجيل الدخول
- **Logout**: تسجيل الخروج
- **Lockout**: صفحة قفل الحساب (بعد محاولات فاشلة)
- **AccessDenied**: صفحة رفض الوصول

#### 3. **CategoriesController** 🔒

- **Index**: عرض جميع الفئات
- **Details**: عرض تفاصيل فئة معينة
- **Create**: إضافة فئة جديدة
- **Edit**: تعديل فئة موجودة
- **Delete**: حذف فئة
- يحتاج تسجيل دخول (`[Authorize]`)

#### 4. **ProductsController** 🔒

- إدارة كاملة للمنتجات (CRUD)
- عرض المنتجات مع الفئة المرتبطة بها
- يحتاج تسجيل دخول

#### 5. **ClientsController** 🔒

- إدارة معلومات العملاء
- عرض العملاء مع عناوينهم وطلباتهم
- يحتاج تسجيل دخول

#### 6. **OrdersController** 🔒

- إدارة طلبات العملاء
- عرض الطلبات مع العميل والحالة والعناصر
- يحتاج تسجيل دخول

#### 7. **VendorsController** 🔒

- إدارة معلومات الموردين
- متابعة الموردين النشطين والمحظورين
- يحتاج تسجيل دخول

#### 8. **SupplyOrdersController** 🔒

- إدارة أوامر التوريد من الموردين
- عرض الأوامر مع تفاصيل المنتجات
- يحتاج تسجيل دخول

#### 9. **UsersController** 🔒

- إدارة حسابات المستخدمين في النظام
- يحتاج تسجيل دخول

---

## 🔐 نظام المصادقة (Authentication)

### كيف يعمل نظام تسجيل الدخول؟

1. **التسجيل (Register)**:

   - المستخدم يدخل البريد الإلكتروني وكلمة المرور
   - النظام يتحقق من صحة البيانات
   - يتم تشفير كلمة المرور
   - يتم إنشاء حساب جديد في قاعدة البيانات
   - تسجيل دخول تلقائي بعد التسجيل

2. **تسجيل الدخول (Login)**:

   - المستخدم يدخل البريد الإلكتروني وكلمة المرور
   - النظام يتحقق من البيانات
   - إذا كانت صحيحة، يتم إنشاء جلسة (Session)
   - المستخدم يصبح قادرًا على الوصول للصفحات المحمية

3. **الحماية (`[Authorize]`)**:

   - أي متحكم أو صفحة عليها `[Authorize]` تحتاج تسجيل دخول
   - إذا حاول مستخدم غير مسجل الدخول، يتم تحويله لصفحة Login

4. **الأمان**:
   - كلمات المرور مشفرة
   - قفل الحساب بعد 5 محاولات فاشلة لمدة 5 دقائق
   - كلمة المرور يجب أن تحتوي على: حرف كبير، حرف صغير، رقم، وطول 6 أحرف على الأقل

---

## 🎨 الواجهات (Views)

### كيف تعمل الواجهات؟

1. **Layout (\_Layout.cshtml)**:

   - التصميم الرئيسي لجميع الصفحات
   - يحتوي على القائمة العلوية (Navbar)
   - يعرض معلومات المستخدم المسجل
   - أزرار Login/Register للزوار
   - زر Logout للمستخدمين المسجلين

2. **Views لكل Controller**:

   - **Index.cshtml**: صفحة العرض الرئيسية (قائمة)
   - **Details.cshtml**: عرض تفاصيل عنصر واحد
   - **Create.cshtml**: نموذج إضافة جديد
   - **Edit.cshtml**: نموذج تعديل
   - **Delete.cshtml**: تأكيد الحذف

3. **Account Views**:
   - **Login.cshtml**: نموذج تسجيل الدخول
   - **Register.cshtml**: نموذج التسجيل
   - **Lockout.cshtml**: صفحة قفل الحساب
   - **AccessDenied.cshtml**: صفحة رفض الوصول

---

## ⚙️ ملف Program.cs

### ماذا يفعل هذا الملف؟

```csharp
// 1. إنشاء التطبيق
var builder = WebApplication.CreateBuilder(args);

// 2. تسجيل الخدمات (Services)
builder.Services.AddControllersWithViews();  // إضافة MVC

// 3. إضافة قاعدة البيانات
builder.Services.AddDbContext<ShoppiaContext>(options =>
    options.UseSqlServer(connectionString));

// 4. إضافة نظام Identity للمصادقة
builder.Services.AddIdentity<ApplicationUser, IdentityRole>(options => {
    // إعدادات كلمة المرور
    // إعدادات قفل الحساب
    // إلخ...
})
.AddEntityFrameworkStores<ShoppiaContext>();

// 5. بناء التطبيق
var app = builder.Build();

// 6. إضافة Middleware (البرمجيات الوسيطة)
app.UseHttpsRedirection();      // تحويل HTTP إلى HTTPS
app.UseRouting();               // تفعيل التوجيه
app.UseAuthentication();        // تفعيل المصادقة
app.UseAuthorization();         // تفعيل الصلاحيات

// 7. تحديد المسارات
app.MapControllerRoute(
    name: "default",
    pattern: "{controller=Home}/{action=Index}/{id?}");

// 8. تشغيل التطبيق
app.Run();
```

---

## 🔄 كيف يعمل التطبيق؟ (Request Flow)

### مثال: عرض قائمة المنتجات

1. **المستخدم يزور**: `https://localhost:5000/Products`

2. **التوجيه (Routing)**: النظام يحدد أن الطلب لـ `ProductsController.Index()`

3. **المصادقة (Authentication)**:

   - النظام يتحقق أن `ProductsController` عليه `[Authorize]`
   - إذا لم يكن مسجل، يحوله لصفحة Login
   - إذا كان مسجل، يكمل

4. **المتحكم ينفذ**:

   ```csharp
   public async Task<IActionResult> Index()
   {
       var products = await _context.Products
           .Include(p => p.Category)  // جلب الفئة مع كل منتج
           .ToListAsync();
       return View(products);         // إرسال البيانات للعرض
   }
   ```

5. **العرض يرسل HTML**: يتم عرض `Views/Products/Index.cshtml` مع البيانات

6. **المستخدم يرى**: جدول أو قائمة بالمنتجات

---

## 📝 ViewModels

### لماذا نستخدم ViewModels؟

بدلاً من إرسال Model قاعدة البيانات مباشرة للعرض، نستخدم ViewModel خاص يحتوي فقط على البيانات المطلوبة.

**مثال**:

```csharp
// LoginViewModel
public class LoginViewModel
{
    public string Email { get; set; }
    public string Password { get; set; }
    public bool RememberMe { get; set; }
}
```

**الفوائد**:

- أمان أفضل (لا نعرض كل بيانات المستخدم)
- تحقق من البيانات (Validation)
- سهولة الاستخدام

---

## 🗂️ Entity Framework Core

### ما هو Entity Framework؟

- مكتبة للتعامل مع قاعدة البيانات بطريقة سهلة
- بدلاً من كتابة SQL، نستخدم C# مباشرة

### أمثلة على العمليات:

```csharp
// قراءة جميع المنتجات
var products = await _context.Products.ToListAsync();

// قراءة منتج واحد
var product = await _context.Products.FindAsync(id);

// إضافة منتج جديد
_context.Products.Add(newProduct);
await _context.SaveChangesAsync();

// تعديل منتج
_context.Products.Update(product);
await _context.SaveChangesAsync();

// حذف منتج
_context.Products.Remove(product);
await _context.SaveChangesAsync();

// قراءة مع علاقات
var product = await _context.Products
    .Include(p => p.Category)       // جلب الفئة
    .Include(p => p.OrderItems)     // جلب عناصر الطلبات
    .FirstOrDefaultAsync(p => p.ProductId == id);
```

---

## 🎯 الميزات الرئيسية

### 1. **CRUD Operations**

كل متحكم يوفر عمليات:

- **C**reate (إنشاء)
- **R**ead (قراءة)
- **U**pdate (تحديث)
- **D**elete (حذف)

### 2. **Authentication & Authorization**

- نظام تسجيل دخول كامل
- حماية الصفحات المهمة
- إدارة الجلسات

### 3. **Entity Relationships**

- علاقة واحد لكثير (One-to-Many): فئة → منتجات
- علاقة كثير لكثير (Many-to-Many): مستخدمين ← → أدوار

### 4. **Responsive Design**

- استخدام Bootstrap للتصميم
- يعمل على جميع الأجهزة (موبايل، تابلت، كمبيوتر)

---

## 🚀 كيفية تشغيل المشروع

### 1. **المتطلبات**:

- .NET 9.0 SDK
- SQL Server
- Visual Studio أو VS Code

### 2. **إعداد قاعدة البيانات**:

```bash
# إنشاء Migration للـ Identity
dotnet ef migrations add AddIdentity

# تحديث قاعدة البيانات
dotnet ef database update
```

### 3. **تشغيل التطبيق**:

```bash
dotnet run
```

### 4. **فتح المتصفح**:

```
https://localhost:5001
```

---

## 📌 ملاحظات مهمة

### Security Settings:

- كلمة المرور: 6 أحرف على الأقل، حرف كبير + صغير + رقم
- قفل الحساب: 5 محاولات فاشلة = قفل 5 دقائق
- الجلسة: تستمر 7 أيام مع خيار "تذكرني"

### Connection String:

موجود في `appsettings.json`:

```json
{
	"ConnectionStrings": {
		"DefaultConnection": "Server=...;Database=Shoppia;..."
	}
}
```

### .gitignore:

- `bin/` و `obj/` محذوفة من Git
- ملفات Build لا تُرفع للمستودع
- يحافظ على المشروع نظيف

---

## 🎓 المفاهيم المستخدمة

1. **MVC Pattern**: فصل المنطق (Model) عن العرض (View) عن التحكم (Controller)
2. **Dependency Injection**: حقن الخدمات تلقائيًا في المتحكمات
3. **Async/Await**: برمجة غير متزامنة لأداء أفضل
4. **Data Annotations**: التحقق من البيانات باستخدام Attributes
5. **Identity Framework**: نظام مصادقة جاهز من Microsoft

---

## 📚 للتعلم أكثر

- **ASP.NET Core MVC**: https://docs.microsoft.com/aspnet/core/mvc
- **Entity Framework Core**: https://docs.microsoft.com/ef/core
- **ASP.NET Core Identity**: https://docs.microsoft.com/aspnet/core/security/authentication/identity

---

## ✅ الخلاصة

هذا المشروع يوفر نظام إدارة متجر إلكتروني كامل مع:

- إدارة المنتجات والفئات
- إدارة العملاء والطلبات
- إدارة الموردين وأوامر التوريد
- نظام مصادقة آمن
- واجهات سهلة الاستخدام

كل شيء مبني على أفضل الممارسات في البرمجة وجاهز للتطوير والتوسع! 🚀
